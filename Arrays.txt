
### 🟢 **Easy (Start Here)**

1. **Find max and min in an array**
2. **Reverse the array**
3. **Check if array is sorted**
4. **Count number of even and odd numbers**
5. **Find the second largest element**
6. **Remove duplicates from a sorted array**

---

### 🟡 **Medium (Logic-based)**

7. **Left rotate array by 1**
8. **Left rotate array by k positions**
9. **Move all zeroes to the end**
10. **Find the longest subarray with sum = K**
11. **Kadane’s Algorithm (max sum subarray)**
12. **Find the majority element (appears > n/2 times)**

---

### 🔴 **Hard (When confident)**

13. **Find the element that appears once when all others appear twice**
14. **Trapping rain water (hard logic)**
15. **Maximum product subarray**
16. **Find all pairs with a given sum**
17. **Find missing number in 1 to n**
18. **Rearrange positive and negative numbers alternately**















These 18 problems you solved are **excellent** for beginners + intermediate…
But if you want to be **strong in arrays for DSA + interviews**, you need a few more **logical + pattern-based** problems.

Below is the **complete logical progression**.

---

# ⭐ **Must-Solve Logical Problems (Next Level Arrays)**

These are more **thinking + logic based**, very common in interviews.

---

## 🟢 **Easy-Medium Logic (Highly Important)**

### 1️⃣ Find **frequency of each element**

(no extra space or using HashMap — try both ways)

### 2️⃣ Count pairs with **difference = K**

Example: arr = {1,5,3,4,2}, K=2 → pairs = (1,3), (3,5), (2,4)

### 3️⃣ Find **first repeating** and **first non-repeating** element

### 4️⃣ Count **subarrays with sum = K**

(Positive + negative integers version)

### 5️⃣ Find all **leaders** in an array

Leader = element that is greater than all elements to its right.

### 6️⃣ Find **equilibrium index**

(where left sum == right sum)

### 7️⃣ Find **peak element**

(element > neighbors)

### 8️⃣ Find **missing AND repeating number** together

arr size = n, elements = 1..n

---

## 🟡 **Medium-Hard Logic (Very Important)**

### 9️⃣ **Dutch National Flag Problem**

Sort array of 0,1,2 without using sorting.

### 🔟 **Maximum consecutive 1s** when you can flip at most K zeros.

### 1️⃣1️⃣ Sliding window problems

* longest substring with ≤ k distinct
* longest window with sum ≤ K
* min size subarray with sum ≥ K

### 1️⃣2️⃣ Two-pointer problems

* 3Sum
* 3Sum closest
* Container with most water

### 1️⃣3️⃣ Stock Buy & Sell variations

* max profit once
* max profit unlimited
* with transaction fee
* with cooldown

### 1️⃣4️⃣ Find **minimum number of jumps** to reach end

---

## 🔴 **Hard Logical Problems (Interview Favorites)**

### 1️⃣5️⃣ Merge overlapping intervals

### 1️⃣6️⃣ Find **Kth smallest/largest** (QuickSelect logic)

### 1️⃣7️⃣ Maximum sum subarray of size K

(sliding window variations)

### 1️⃣8️⃣ Rotate matrix 90°

(in-place matrix rotation)

### 1️⃣9️⃣ Spiral matrix

(print or create)

### 2️⃣0️⃣ Set Matrix Zeroes

(O(1) space tricky logic)

---

# ⭐ So… Are the previous 18 enough?

**YES for exams**
**NO for interviews** (need more logical thinking problems)

If your goal =
✔ **College exams** → Previous 18 are enough
✔ **Placement / DSA Strong** → Solve these 20 logical problems

---

# 👍 I can give you

➡ **One-by-one simple Java code**
➡ **In easy → hard order**
➡ **With diagrams & logic explanation**

Just say:

👉 **"Give me next logical problem"**
or
👉 **"Start logical problems 1 by 1"**

indiabix